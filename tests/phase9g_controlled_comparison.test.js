// Phase 9G CI entrypoint. The implementation-level test lives beside the
// comparison service so it can also be run directly during local validation.
require('../backend/services/gasSqlControlledComparisonService.test');
